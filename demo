我现在在写工作周报，下面是我之前写的一些内容，请根据这个内容风格重新写一下这周的周报。
上周的周报：
总目标
目标一：面向智算系统可靠性运维能力构建，负责系统网络层状态模型构建&网络问题识别、影响分析与定位，输出《模块算法方案设计说明书》。支撑整体建模准确率85%，问题识别、影响分析与定位准确率85%，助力能力落入智算系统运维服务规划。
目标二：负责构建基于网络DTN交互的LLM查询代码生成，输出《方案设计说明书》，准确率达80%。支撑面向管理服务LLM应用生态构建。

一、推动项目进展
1. 针对目标1：
1）对自建集群（128卡）&南京集群（256卡）基于硬件网络拓扑层，实现静态NPU间业务路径层计算，输出至“静态NPU间业务路径层计算”；
2）基于hccn_tool网络状态检查命令，实现单node内NPU互联网络状态日志收集、保存、解析，输出至“自建集群hccn_tool日志收集工具”；
3）基于以上输出，对于2个实际问题（自建&南京集群）案例进行验证，并支撑输出2个案例中根据报错NPU节点定位到影响的NPU间业务路径的分析

2. 针对目标2：
1）通过对welink上pangu-s的ms-helper插件进行试验，了解ms-helper如何进行文件上传，需求理解，需求重构，python代码生成和运行

这周的内容：
目标一：
1. 针对20240313的全量notify wait案例进行深度挖掘，得出故障链，深入了解并掌握notify wait和event wait的机制，深入了解并掌握cqe的机制，得出案例中event wait和notify wait出现的原因，输出至“自建集群-20240313-全量notify wait”
2. 开发组件：使用多进程和分割文件夹的方法大大加快plog日志grep的运行效率并将提取的结果按照时序进行排列，输出至“grep_多进程”
3. 针对20240328的event wait案例进行定位，与HCCL同事沟通了解发现之前从未出现过的干扰ERROR，并设计组件进行过滤，但是plog暂时没有解析到能用的结果，train_log找到疑似有问题的节点，但是还没有确定，输出至“过滤cqe error[21]”
4. 完成’ai集群算法技术详细说明书-V0.1‘中plog数据预处理子模块，device_log数据预处理子模块，业务路径层还原子模块的初稿，输出至“集群算法技术详细说明书-V0.1”
5. 调研目前自建集群的mindx-dl工具有哪些数据，还差哪些数据，输出至“NPU监控指标信息”
6. 开发组件：从Prometheus平台获取历史一个时间段内的NPU数据
7. 

目标二：
1. 学习并使用langgraph单agent框架进行DTN数据分析能力的开发任务，并将思路结果进行展示，在会上进行讨论，输出至“langgraph单agent框架”
2. 研究如何将ms的数据处理功能迁移到DTN上，与前端同事得出初步方案和相应的工作量，输出至“DTN目前工作量”
3. 与小组成员讨论并得出之后在langgraph上需要进行实验的内容（multi-agent）

